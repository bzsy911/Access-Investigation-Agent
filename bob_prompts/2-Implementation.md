# Session 2 — Implementation

## Context

The architecture and tool design have been finalised in `PROJECT.md`. Read that
file before proceeding.

## Your task

Implement the full project as specified in `PROJECT.md`, including:

- All source files described in the **Repository Layout** section.
- A test suite (not listed in the layout, but required). Write it in
  `tests/test_tools.py` and cover all tool functions as described in the
  **Testing Approach** section.

Follow the design decisions in `PROJECT.md` exactly. Do not add features or
abstractions beyond what is specified.
